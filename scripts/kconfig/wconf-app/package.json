{
    "name": "wconf-frontend",
    "version": "0.1.0",
    "description": "Frontend for the Tauri-based kernel configuration tool",
    "private": true,
    "scripts": {
        "dev": "vite",
        "prebuild": "node scripts/copy-files.js",
        "build": "vite build --emptyOutDir",
        "preview": "vite preview",
        "build:prod": "vite build --mode production"
    },
    "dependencies": {
        "@tauri-apps/api": "^2.8.0"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.8.0",
        "vite": "^4.0.0"
    }
}
